# Example config.toml — generated by `sec init` with the defaults shown.
db_path = "~/.cli-secret/vault.db"
host = "127.0.0.1"
port = 9090
auto_lock_minutes = 0

[backup]
  enabled = true
  dir = "~/.cli-secret/backups"
  keep = 10
